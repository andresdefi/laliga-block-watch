"""LaLiga match schedule fetcher.

Pulls the current season schedule and exposes `is_match_window(ts)` for the
detection engine to correlate reachability drops with broadcast windows.

Not implemented.
"""
